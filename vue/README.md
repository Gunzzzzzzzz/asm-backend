# Vue + Bootstrap demo

Quick start (Windows PowerShell):

```powershell
cd vue
npm install
npm run dev
```

- This project uses Vite + Vue 3. Ensure `@vitejs/plugin-vue` is installed (declared in package.json).
- The app uses Bootstrap via CDN in `index.html`.
